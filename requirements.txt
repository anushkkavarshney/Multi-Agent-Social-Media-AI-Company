# Prodigal AI Task 1 — pinned dependencies (Python 3.10 compatible)
# Installed into .venv via: pip install -r requirements.txt
#
# NOTE ON PYTHON VERSION: architecture doc targets 3.11+; this machine only has
# 3.10.11 and 3.14.0. We build on 3.10 with these pins; all library APIs we use
# exist identically on 3.11+. Documented as a deviation in SETUP_LOG.md.

# --- Web framework (mock social platform API) ---
fastapi==0.115.6
uvicorn[standard]==0.32.1          # ASGI server; [standard] adds uvloop/watchfiles where available

# --- Persistence (SQLite, async) ---
sqlalchemy==2.0.36
aiosqlite==0.20.0                  # async driver for SQLAlchemy's sqlite+aiosqlite dialect

# --- Config & validation ---
pydantic==2.10.4    
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- HTTP client (Ollama API + tests) ---
httpx==0.28.1

# --- Simulation statistics ---
numpy==1.26.4                      # last 1.x line: supports 3.10, keeps negative_binomial API stable
scipy==1.11.4                      # stats tests (mannwhitneyu) for the hidden-rule direction checks

# --- CLI / demo runner (used from Day 2 onward; pinned now for reproducibility) ---
typer==0.15.1
rich==13.9.4

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
